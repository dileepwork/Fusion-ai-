Session cookies are stored here automatically.

Files: sessions/{username}.pkl

These are created by SessionManager.save() after each successful login.
Delete a .pkl file to force a fresh login on next run.

DO NOT commit this folder to git — add 'sessions/' to .gitignore.
